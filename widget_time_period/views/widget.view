<?php
/**
 * Widget d'affichage minimal (aucune dépendance).
 * Il doit *toujours* s'afficher sans logique externe.
 */
(new CWidgetView($data))
  ->addItem(new CTag('div', true, 'Time period widget — baseline OK'))
  ->show();